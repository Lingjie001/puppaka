<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title ? title + ' | ' : '' %><%= siteName %></title>
  <meta name="description" content="<%= description || siteDescription %>">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  
  <!-- CSS -->
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/pages.css">
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🐾</text></svg>">
  
  <!-- Open Graph -->
  <meta property="og:title" content="<%= title || siteName %>">
  <meta property="og:description" content="<%= description || siteDescription %>">
  <meta property="og:type" content="website">
  
  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="<%= title || siteName %>">
  <meta name="twitter:description" content="<%= description || siteDescription %>">
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar">
    <div class="nav-container">
      <a href="/" class="logo">
        <span class="logo-icon">🐾</span>
        <span class="logo-text"><%= siteName %></span>
      </a>
      
      <button class="nav-toggle" aria-label="Toggle menu">
        <span class="hamburger"></span>
      </button>
      
      <ul class="nav-menu">
        <li class="nav-item">
          <a href="/" class="nav-link <%= path === '/' ? 'active' : '' %>">首页</a>
        </li>
        <li class="nav-item">
          <a href="/blog" class="nav-link <%= path === '/blog' ? 'active' : '' %>">博客</a>
        </li>
        <li class="nav-item">
          <a href="/portfolio" class="nav-link <%= path === '/portfolio' ? 'active' : '' %>">作品</a>
        </li>
        <li class="nav-item">
          <a href="/about" class="nav-link <%= path === '/about' ? 'active' : '' %>">关于</a>
        </li>
        <li class="nav-item">
          <a href="/contact" class="nav-link <%= path === '/contact' ? 'active' : '' %>">联系</a>
        </li>
      </ul>
      
      <% if (typeof user !== 'undefined' && user) { %>
      <a href="/admin" class="admin-link">后台</a>
      <% } %>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="main-content">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-container">
      <div class="footer-content">
        <div class="footer-brand">
          <span class="logo-icon">🐾</span>
          <span class="logo-text"><%= siteName %></span>
        </div>
        <p class="footer-desc">Personal Blog & Portfolio</p>
        <div class="footer-links">
          <a href="https://github.com/Lingjie001" target="_blank" rel="noopener">GitHub</a>
          <a href="mailto:guanghui0711@gmail.com">Email</a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>© <%= new Date().getFullYear() %> <%= siteName %>. All rights reserved.</p>
        <% if (typeof user === 'undefined' || !user) { %>
        <a href="/admin/login" class="admin-login">Admin</a>
        <% } %>
      </div>
    </div>
  </footer>

  <!-- Scripts -->
  <script src="/js/main.js"></script>
</body>
</html>